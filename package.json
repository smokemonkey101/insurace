{
  "name": "mom-pdf-extractor",
  "version": "0.1.0",
  "private": true,
  "description": "Railway-ready TypeScript Node website for medication PDF extraction.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node dist/server.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
